{
  "name": "@svajna/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": { "svajna": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@svajna/core": "0.1.0" }
}
