{
  "name": "@svajna/mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": { "svajna-mcp": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.3",
    "@svajna/core": "0.1.0",
    "zod": "^3.24.1"
  }
}
